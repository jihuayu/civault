{
  "name": "civault-actions",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/load.ts src/exec.ts --outdir=dist --platform=node --target=node24 --bundle --format=cjs --legal-comments=eof --out-extension:.js=.cjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "license": "Apache-2.0"
}
